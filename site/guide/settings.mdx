---
sidebar_position: 10
---

# Settings

## Reset

export function ResetButton() {
  return (
    <button
      className="button button--danger"
      onClick={() => {
        localStorage.clear();
        window.location.reload();
      }}
    >
      Reset
    </button>
  );
}

<ResetButton />
